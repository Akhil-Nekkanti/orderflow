[package]
name = "bench"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Dependency-free microbenchmark suite for the engine and codec"

[dependencies]
lob = { path = "../lob" }
wire = { path = "../wire" }
